-- Create the table
CREATE TABLE test_table( 
	id int,
	first_name text,
	last_name text
);

-- Insert various records to the table
insert into test_table(
	id,
	first_name,
	last_name
) values (
	1,
	'John',
	'Doe'
);
